/*
Auth Terminology:
-----------------
"register" or "sign up" (first time create a user)
"Login" or "SignIn" (already you have a user account created)

*/

/*
    get form value:
    ---------------
    set form onSubmitHandler
    set event parameter
    event.target.email.value( for the get the value)

*/